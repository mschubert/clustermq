# Register clustermq as \`foreach\` parallel handler

Register clustermq as \`foreach\` parallel handler

## Usage

``` r
register_dopar_cmq(...)
```

## Arguments

- ...:

  List of arguments passed to the \`Q\` function, e.g. n_jobs

before_install: 
- sudo apt-get install libzmq3-dev
# - sudo wget -P /usr/local/bin
# https://bitbucket.org/dalloliogm/fake-qsub/raw/default/{b,q}sub
# - sudo chmod a+x /usr/local/bin/{b,q}sub

install:
 - R -e "install.packages('devtools')" \
     -e "devtools::install_github('mschubert/rzmq',ref='msgreuse')"

language: r

warnings_are_errors: false

cache: packages
